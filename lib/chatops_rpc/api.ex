defmodule ChatopsRPC.Api do
  def info(endpoint) do
    # with {:ok, json} <- get(endpoint),
    #      {:ok, rpcs} <- parse(json) do
    #   {:ok, rpcs}
    # end
  end

  defp get(endpoint) do
  end
end

